import { Gpio } from "pigpio"

type DistanceCallback = (number) => void;
export class DistanceHelper {
    constructor() {
        
    }

    async function subscribeDistanceChanges(callback: DistanceCallback) {
    
    }
}